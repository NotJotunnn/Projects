const weirdBtn = document.getElementById("weird");

weirdBtn.addEventListener("click", () => {
        window.consoleHelloAPI.console();
        // const darkmode = window.consoleHelloAPI.answer;
        // TO-DO: Discover how to get the data from main here, and apply it
});